import Component from '@ember/component';

export default Component.extend({
  tagName: 'li',
  classNames: ['bg-white', 'border-b', 'border-gray-400', 'flex', 'flex-col', 'sm:flex-row', 'p-8', 'items-center'],
});
